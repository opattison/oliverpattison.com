---
layout: base.njk
permalink: /stories/{{ page.fileSlug }}/
---

{% if banner %}
  <section class="grid--banner">
    {% set src = banner.src %}
    {% set alt = banner.alt %}
    <figure class="grid-item--photo grid-item--photo--full">
    {% photo src, alt %}
    </figure>
  </section>
{% endif %}
<div class="page page--photo">
  <div class="boundary">
    <main>
    {% if entry %}
      <section class="entry">{% markdown entry %}</section>
    {% endif %}
      <section class="grid--photo">
      {% for image in images %}
        {% set src = image.src %}
        {% set alt = image.alt %}
        {% set style = image.style %}
        
        <figure class="grid-item--photo{% if style %} grid-item--photo--{{ style }}{% endif %}">
        {% photo src, alt %}
        </figure>
      {% endfor %}
      </section>
      
      <section class="content--photo">
      {{ content | safe }}
      </section>
    </main>
    {% include 'nav-list--home.njk' %}
  </div>
</div>